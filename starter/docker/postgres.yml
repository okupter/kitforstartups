version: '3'

services:
  kitforstartups-postgres:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_USER: postgres
      POSTGRES_DB: kitforstartups-postgres
    ports:
      - 5432:5432
