.DS_Store
node_modules
/build
/.svelte-kit
/package
# this is being deployed through node adapter with easypanel
# easypanel allows creating an .env file with the environment variables
# so we don't need to ignore it
# .env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
src/lib/server/db/data.sqlite
